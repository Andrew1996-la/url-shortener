env: "local" #dev, prod, local
storage_path: "storage/storage.db" # путь до базы данных
http_server:
  address: "localhost:8082" # порт запуска приложения
  timeout: 4s # время на получение запроса или на ответ
  idle_timeout: 60s # время жизни соеденения с клиентом